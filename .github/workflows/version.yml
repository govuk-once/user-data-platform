name: Create Release

on:
  workflow_call:
    outputs:
      version:
        description: 'The new semantic version number'
        value: ${{ jobs.version.outputs.version }}
      type:
        description: 'The type of release (major, minor, patch)'
        value: ${{ jobs.version.outputs.type }}
      notes:
        description: 'The generated markdown release notes'
        value: ${{ jobs.version.outputs.notes }}

env:
  NODE_VERSION: '22'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  version:
    runs-on: ubuntu-latest
    outputs:
      version: "1.41.1"
      type: minor
      notes: |
        ## Test release notes
        - Change 1
        - Change 2
    #   version: ${{ steps.release.outputs.semVer }}
    #   type: ${{ steps.release.outputs.type }}
    #   notes: ${{ steps.release.outputs.notes }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Generate GitHub App Token
        id: generate-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.GH_UDPTAGRELEASE_GOVUK_ONCE_APP_ID }}
          private-key: ${{ secrets.GH_UDPTAGRELEASE_GOVUK_ONCE_PRIVATE_KEY }}

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9

      - name: Setup node
        uses: actions/setup-node@v6
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm i --frozen-lockfile

      - name: Run semantic release
        id: release
        run: pnpm run semantic:release
        env:
          GITHUB_TOKEN: ${{ steps.generate-token.outputs.token }}

      - name: Release Outputs
        run: |
          echo "The next version is ${{ steps.release.outputs.semVer }}"
          echo "The release type is ${{ steps.release.outputs.type }}"
          echo "The release notes are ${{ steps.release.outputs.notes }}"
