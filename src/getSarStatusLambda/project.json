{
  "name": "@src/getSarStatusLambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "VITEST_COVERAGE_DIR=coverage/getSarStatusLambda vitest run src/getSarStatusLambda/*"
      }
    },
    "build": {
      "dependsOn": ["typecheck", "test"],
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{option.outputPath}"],
      "options": {
        "main": "src/getSarStatusLambda/handler.ts",
        "tsConfig": "src/getSarStatusLambda/tsconfig.json",
        "outputPath": "build/getSarStatusLambda",
        "minify": true,
        "bundle": true,
        "thirdParty": true,
        "platform": "node",
        "declaration": false,
        "target": "node20",
        "format": ["cjs"],
        "external": ["@aws-sdk/*"],
        "skipTypeCheck": true,
        "esbuildOptions": {
          "outfile": "./build/getSarStatusLambda/index.js"
        }
      }
    }
  }
}
