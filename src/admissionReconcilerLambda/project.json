{
  "name": "@src/admissionReconcilerLambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "VITEST_COVERAGE_DIR=coverage/admissionReconcilerLambda vitest run src/admissionReconcilerLambda/*"
      }
    },
    "build": {
      "dependsOn": ["typecheck", "test"],
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{option.outputPath}"],
      "options": {
        "main": "src/admissionReconcilerLambda/handler.ts",
        "tsConfig": "src/admissionReconcilerLambda/tsconfig.json",
        "outputPath": "build/admissionReconcilerLambda",
        "minify": true,
        "bundle": true,
        "thirdParty": true,
        "platform": "node",
        "declaration": false,
        "target": "node20",
        "format": ["cjs"],
        "external": ["@aws-sdk/*"],
        "skipTypeCheck": true,
        "esbuildOptions": {
          "outfile": "./build/admissionReconcilerLambda/index.js"
        }
      }
    }
  }
}
