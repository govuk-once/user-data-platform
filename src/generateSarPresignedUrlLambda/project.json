{
  "name": "@src/generateSarPresignedUrlLambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run src/generateSarPresignedUrlLambda/*"
      }
    },
    "build": {
      "dependsOn": ["typecheck", "test"],
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{option.outputPath}"],
      "options": {
        "main": "src/generateSarPresignedUrlLambda/handler.ts",
        "tsConfig": "src/generateSarPresignedUrlLambda/tsconfig.json",
        "outputPath": "build/generateSarPresignedUrlLambda",
        "minify": true,
        "bundle": true,
        "thirdParty": true,
        "platform": "node",
        "declaration": false,
        "target": "node20",
        "format": ["cjs"],
        "external": ["@aws-sdk/*"],
        "skipTypeCheck": true,
        "esbuildOptions": {
          "outfile": "./build/generateSarPresignedUrlLambda/index.js"
        }
      }
    }
  }
}
