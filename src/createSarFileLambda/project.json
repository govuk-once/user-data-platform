{
  "name": "@src/createSarFileLambda",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run src/createSarFileLambda/*"
      }
    },
    "build": {
      "dependsOn": ["typecheck", "test"],
      "executor": "@nx/esbuild:esbuild",
      "outputs": ["{option.outputPath}"],
      "options": {
        "main": "src/createSarFileLambda/handler.ts",
        "tsConfig": "src/createSarFileLambda/tsconfig.json",
        "outputPath": "build/createSarFileLambda",
        "minify": true,
        "bundle": true,
        "thirdParty": true,
        "platform": "node",
        "declaration": false,
        "target": "node20",
        "format": ["cjs"],
        "external": ["@aws-sdk/*"],
        "skipTypeCheck": true,
        "esbuildOptions": {
          "outfile": "./build/createSarFileLambda/index.js"
        }
      }
    }
  }
}
