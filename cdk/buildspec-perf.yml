version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - curl -sL https://github.com/grafana/k6/releases/download/v0.55.0/K6-v0.55.0-linux-amd64.tar.gz | tar xz
      - mv k6-v0.55.0-linux-amd64/k6 /usr/local/bin
      - k6 version

  pre_build:
    commands:
      - echo "Setting up performance tests"
      - rm -rf performance/dist
      - set +x && eval "$(node performance/scripts/resolve-credentials.mjs)" > /dev/null 2>&1 && set -x"
      - echo "AWS credentials resolved"

  build:
    commands:
      - echo "Running Performance tests"
      - NX_DAEMON=false NX_PLUGIN_NO_TIMEOUTS=true ./node_modules/.bin/nx run @test/performance:${PERF_TARGET:-smoke} --verbose
      - echo "Performance tests completed"

  post_build:
    commands:
      - chmod +x performance/scripts/publish-metrics.sh
      - performance/scripts/publish-metrics.sh