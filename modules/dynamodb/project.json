{
  "name": "@tf/dynamodb",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": [],
  "targets": {
    "init": {
      "dependsOn": ["@udp:set-developer"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cd ./modules/dynamodb && terraform init --backend-config='backend.tfvars' -reconfigure"
      }
    },
    "plan": {
      "dependsOn": ["init"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cd ./modules/dynamodb && terraform plan -out=tfplan"
      }
    },
    "apply": {
      "dependsOn": ["plan"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cd ./modules/dynamodb && terraform apply tfplan"
      }
    },
    "destroy": {
      "dependsOn": ["@tf/api-gateway:destroy"],
      "executor": "nx:run-commands",
      "options": {
        "command": "cd ./modules/dynamodb && terraform destroy -auto-approve"
      }
    }
  }
}
